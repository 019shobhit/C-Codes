// Given the point (x1, y1), (x2, y2)and (x3, y3), write a program to check if all the three points falls on one straight line.
#include <stdio.h>
#include <math.h>

int main()
{
    int x1, x2, y1, y2, z1, z2;
    printf("enter the cordinates of the first point(x1,x2) : ");
    sacnf("%d%d", &x1, &x2);
    printf("enter the cordinates of the first point(y1,y2) : ");
    sacnf("%d%d", &y1, &y2);
    printf("enter the cordinates of the first point(z1,z2) : ");
    sacnf("%d%d", &z1, &z2);
    double ab,bc,ac,abc;
    ab= 
    return 0;
}
